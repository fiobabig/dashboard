{
  "devDependencies": {
    "@firebase/rules-unit-testing": "^1.3.12",
    "firebase": "^8.8.1",
    "firebase-admin": "^9.11.0",
    "jest": "^27.0.6"
  },
  "scripts": {
    "emu": "firebase emulators:start",
    "rules": "yarn --cwd ./rules test",
    "action_test": "firebase emulators:exec --only firestore jest"
  }
}
